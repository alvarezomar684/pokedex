import React from 'react'
import { HashRouter as Router, Switch } from 'react-router-dom'

export const Rutas = () => {
    return (
        <Switch>
            <Router>
                
            </Router>
        </Switch>
    )
}
